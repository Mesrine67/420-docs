---
title: Lib
sidebar_label: lib
keywords: [qbox, fivem, modules, lib]
description: Documentation for the core library module in 420. This module provides the global qbx table with utility functions for string manipulation, math operations, table handling, and vehicle-related functionality used across resources.
---

# lib

Provides the `qbx` global table with various utility functions.

## Prerequisites

- Lua 5.4 - add `lua54 'yes'` to your fxmanifest.
- [ox_lib](https://coxdocs.dev/ox_lib) - add `@ox_lib/init.lua` to `shared_scripts` in your fxmanifest.

## Installation

Add `@papi_management/modules/lib.lua` to `shared_scripts` in your fxmanifest. Make sure it's below the `@ox_lib/init.lua` script.