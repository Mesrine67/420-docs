---
title: PlayerData
sidebar_label: playerdata
keywords: [qbox, fivem, modules, playerdata]
description: Documentation for the playerdata module in the 420 resource. This module provides functionality for managing and updating player data through the QBX.PlayerData global variable, available on the client side.
---

# playerdata

Adds a `QBX.PlayerData` global and keeps it updated.

## Installation

Add `@papi_management/modules/playerdata.lua` to `client_scripts` in your fxmanifest.